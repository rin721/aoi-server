openapi: 3.0.3
info:
  title: go-scaffold API
  version: "1.0.0"
  description: >
    当前 go-scaffold HTTP 路由重新生成的 API 契约。本文件基于当前有效的
    路由注册以及 handler、service、model 类型，不基于历史遗留路由记录。
servers:
  - url: http://127.0.0.1:9999
    description: 默认本地服务
tags:
  - name: 探针
  - name: 演示 Todo
  - name: IAM 认证
  - name: IAM 账号
  - name: IAM 组织
  - name: IAM 会话
  - name: IAM 审计
  - name: 插件
security:
  - bearerAuth: []
paths:
  /health:
    get:
      tags: [探针]
      summary: 存活探针
      security: []
      responses:
        "200":
          description: 进程存活。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/HealthStatus"
  /ready:
    get:
      tags: [探针]
      summary: 就绪探针
      security: []
      responses:
        "200":
          description: 服务依赖已就绪。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReadyStatus"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /api/v1/demo/todos:
    post:
      tags: [演示 Todo]
      summary: 创建演示 Todo
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTodoRequest"
      responses:
        "201":
          description: 演示 Todo 已创建。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Todo"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"
    get:
      tags: [演示 Todo]
      summary: 查询演示 Todo 列表
      security: []
      responses:
        "200":
          description: 演示 Todo 列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Todo"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/demo/todos/{id}:
    parameters:
      - $ref: "#/components/parameters/TodoID"
    get:
      tags: [演示 Todo]
      summary: 查询单个演示 Todo
      security: []
      responses:
        "200":
          description: 演示 Todo 已找到。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Todo"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    put:
      tags: [演示 Todo]
      summary: 更新演示 Todo
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTodoRequest"
      responses:
        "200":
          description: 演示 Todo 已更新。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Todo"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [演示 Todo]
      summary: 删除演示 Todo
      security: []
      responses:
        "200":
          description: 演示 Todo 已删除。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [deleted]
                        properties:
                          deleted:
                            type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/setup/status:
    get:
      tags: [IAM 认证]
      summary: Check whether first-time admin setup is required
      security: []
      responses:
        "200":
          description: Setup status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SetupStatus"
        "429":
          description: Public auth endpoint rate limit reached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResult"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/setup/initial-admin:
    post:
      tags: [IAM 认证]
      summary: Create the first organization owner when IAM has no users
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitialAdminSetupRequest"
      responses:
        "200":
          $ref: "#/components/responses/TokenPair"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          description: Public auth endpoint rate limit reached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResult"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/signup:
    post:
      tags: [IAM 认证]
      summary: 自助注册并创建首个组织 owner
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignupRequest"
      responses:
        "200":
          $ref: "#/components/responses/TokenPair"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          description: 认证入口触发限流。
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResult"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/login:
    post:
      tags: [IAM 认证]
      summary: 登录并签发令牌
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          $ref: "#/components/responses/TokenPair"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/refresh:
    post:
      tags: [IAM 认证]
      summary: 刷新 access token 和 refresh token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
      responses:
        "200":
          $ref: "#/components/responses/TokenPair"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/logout:
    post:
      tags: [IAM 认证]
      summary: 撤销当前会话
      responses:
        "200":
          description: 会话已撤销。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [loggedOut]
                        properties:
                          loggedOut:
                            type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/switch-org:
    post:
      tags: [IAM 认证]
      summary: 切换当前组织并签发新令牌
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SwitchOrgRequest"
      responses:
        "200":
          $ref: "#/components/responses/TokenPair"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/password/forgot:
    post:
      tags: [IAM 认证]
      summary: 创建密码重置令牌
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordRequest"
      responses:
        "200":
          description: 重置通知已创建。debug/noop/local 通知驱动会在响应中返回调试 token/link。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/password/reset:
    post:
      tags: [IAM 认证]
      summary: 使用重置令牌重置密码
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordRequest"
      responses:
        "200":
          description: 密码已重置。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [reset]
                        properties:
                          reset:
                            type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/mfa/setup:
    post:
      tags: [IAM 账号]
      summary: 创建或轮换 TOTP MFA 密钥
      responses:
        "200":
          description: MFA 密钥和 otpauth URL。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MFASetup"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/auth/mfa/verify:
    post:
      tags: [IAM 账号]
      summary: 验证并启用 TOTP MFA
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyMFARequest"
      responses:
        "200":
          description: MFA 已启用。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [verified]
                        properties:
                          verified:
                            type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/invitations/{token}/accept:
    post:
      tags: [IAM 认证]
      summary: 接受组织邀请
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptInvitationRequest"
      responses:
        "201":
          description: 邀请已接受。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Principal"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/me:
    get:
      tags: [IAM 账号]
      summary: 获取当前用户资料
      responses:
        "200":
          description: 当前用户。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/me/orgs:
    get:
      tags: [IAM 账号]
      summary: 查询当前用户所属组织
      responses:
        "200":
          description: 当前用户所属组织。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Organization"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs:
    get:
      tags: [IAM 组织]
      summary: 查询组织列表
      responses:
        "200":
          description: 组织列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Organization"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      tags: [IAM 组织]
      summary: 创建组织
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrgRequest"
      responses:
        "201":
          description: 组织已创建。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Organization"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    patch:
      tags: [IAM 组织]
      summary: 更新当前组织信息
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrgRequest"
      responses:
        "200":
          description: 组织已更新。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Organization"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/users:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 组织]
      summary: 查询当前组织用户
      responses:
        "200":
          description: 组织用户及角色。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/OrganizationUser"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/users/{userId}:
    parameters:
      - $ref: "#/components/parameters/OrgID"
      - name: userId
        in: path
        required: true
        schema:
          type: string
    patch:
      tags: [IAM 组织]
      summary: 更新成员状态或角色
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: 成员已更新。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OrganizationUser"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/users/invitations:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    post:
      tags: [IAM 组织]
      summary: 邀请用户加入当前组织
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteUserRequest"
      responses:
        "201":
          description: 邀请已创建。debug/noop/local 通知驱动会在响应中返回调试 token/link。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/invitations:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 组织]
      summary: 查询当前组织邀请
      responses:
        "200":
          description: 邀请列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Invitation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/invitations/{invitationId}:
    parameters:
      - $ref: "#/components/parameters/OrgID"
      - name: invitationId
        in: path
        required: true
        schema:
          type: string
    delete:
      tags: [IAM 组织]
      summary: 撤销待处理邀请
      responses:
        "200":
          description: 邀请已撤销。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [revoked]
                        properties:
                          revoked:
                            type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/roles:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 组织]
      summary: 查询当前组织角色
      responses:
        "200":
          description: 角色列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Role"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      tags: [IAM 组织]
      summary: 在当前组织创建角色
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoleRequest"
      responses:
        "201":
          description: 角色已创建。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/roles/{roleId}:
    parameters:
      - $ref: "#/components/parameters/OrgID"
      - name: roleId
        in: path
        required: true
        schema:
          type: string
    patch:
      tags: [IAM 组织]
      summary: 更新自定义角色
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoleRequest"
      responses:
        "200":
          description: 角色已更新。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/permissions:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 组织]
      summary: 查询可用权限
      responses:
        "200":
          description: 权限列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Permission"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/sessions:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 会话]
      summary: 查询当前用户或指定用户的会话
      parameters:
        - name: userId
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: 会话列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Session"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/sessions/{sessionId}:
    parameters:
      - $ref: "#/components/parameters/OrgID"
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
    delete:
      tags: [IAM 会话]
      summary: 撤销当前组织中的会话
      responses:
        "200":
          description: 会话已撤销。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [revoked]
                        properties:
                          revoked:
                            type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/orgs/{orgId}/audit-logs:
    parameters:
      - $ref: "#/components/parameters/OrgID"
    get:
      tags: [IAM 审计]
      summary: 查询当前组织审计日志
      parameters:
        - name: action
          in: query
          required: false
          schema:
            type: string
        - name: userId
          in: query
          required: false
          schema:
            type: string
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 100
            minimum: 1
      responses:
        "200":
          description: 审计日志列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuditLog"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /api/v1/plugins:
    get:
      tags: [插件]
      summary: 列出插件
      description: 返回当前用户可见菜单已过滤后的插件 manifest。
      responses:
        "200":
          description: 插件列表。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PluginManifest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/plugins/{pluginId}:
    parameters:
      - $ref: "#/components/parameters/PluginID"
    get:
      tags: [插件]
      summary: 读取插件
      responses:
        "200":
          description: 插件 manifest。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PluginManifest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/plugins/{pluginId}/health:
    parameters:
      - $ref: "#/components/parameters/PluginID"
    get:
      tags: [插件]
      summary: 检查插件健康状态
      responses:
        "200":
          description: 插件健康状态。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Result"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PluginHealthStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/plugins/{pluginId}/proxy/{path}:
    parameters:
      - $ref: "#/components/parameters/PluginID"
      - $ref: "#/components/parameters/PluginProxyPath"
    get:
      tags: [插件]
      summary: 代理插件 GET 请求
      responses:
        "200":
          description: 插件 sidecar 原始响应。
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: 插件代理失败。
    post:
      tags: [插件]
      summary: 代理插件 POST 请求
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: 插件 sidecar 原始响应。
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: 插件代理失败。
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    TodoID:
      name: id
      in: path
      required: true
      schema:
        type: integer
        format: uint64
        minimum: 1
    OrgID:
      name: orgId
      in: path
      required: true
      description: 必须与 access token 中的组织 ID 一致。
      schema:
        type: string
    PluginID:
      name: pluginId
      in: path
      required: true
      schema:
        type: string
    PluginProxyPath:
      name: path
      in: path
      required: true
      description: 插件代理路径，必须匹配 manifest proxy.prefixes。
      schema:
        type: string
  responses:
    TokenPair:
      description: 令牌对。
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Result"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TokenPair"
    BadRequest:
      description: 请求输入无效。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResult"
    Unauthorized:
      description: 认证失败或令牌无效。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResult"
    Forbidden:
      description: 已认证主体权限不足。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResult"
    NotFound:
      description: 资源不存在。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResult"
    ServiceUnavailable:
      description: 服务未就绪。
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/ErrorResult"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ReadyStatus"
    InternalServerError:
      description: 服务端内部错误。
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResult"
  schemas:
    Result:
      type: object
      required: [code, message, serverTime]
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        data:
          nullable: true
        traceId:
          type: string
          nullable: true
        serverTime:
          type: integer
          format: int64
          description: Unix 秒级时间戳。
    ErrorResult:
      type: object
      required: [code, message, serverTime]
      properties:
        code:
          type: integer
          description: 来自 types/errors 的错误码。
          example: 1000
        message:
          type: string
        traceId:
          type: string
          nullable: true
        serverTime:
          type: integer
          format: int64
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ok]
    ReadyStatus:
      type: object
      required: [status, checks]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        checks:
          type: object
          additionalProperties:
            type: string
          example:
            database: ok
    Todo:
      type: object
      required: [id, title, completed, createdAt, updatedAt]
      properties:
        id:
          type: integer
          format: uint64
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateTodoRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
          default: false
    UpdateTodoRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
    PluginManifest:
      type: object
      required: [id, name, version, baseURL, healthPath, frontend, menus, permissions, proxy]
      properties:
        id:
          type: string
        name:
          type: string
        version:
          type: string
        baseURL:
          type: string
        healthPath:
          type: string
        frontend:
          type: object
          properties:
            entry:
              type: string
        menus:
          type: array
          items:
            $ref: "#/components/schemas/PluginMenu"
        permissions:
          type: array
          items:
            $ref: "#/components/schemas/PluginPermission"
        proxy:
          type: object
          properties:
            prefixes:
              type: array
              items:
                type: string
        secretRef:
          type: string
    PluginMenu:
      type: object
      required: [code, label, path]
      properties:
        code:
          type: string
        label:
          type: string
        icon:
          type: string
        path:
          type: string
        permission:
          type: string
        order:
          type: integer
    PluginPermission:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
        name:
          type: string
        description:
          type: string
    PluginHealthStatus:
      type: object
      required: [id, status, statusCode, durationMs]
      properties:
        id:
          type: string
        status:
          type: string
          enum: [ok, unhealthy]
        statusCode:
          type: integer
        durationMs:
          type: integer
          format: int64
        error:
          type: string
    LoginRequest:
      type: object
      required: [identifier, password]
      properties:
        identifier:
          type: string
          description: 用户名或邮箱。
        password:
          type: string
          format: password
        orgCode:
          type: string
        mfaCode:
          type: string
    SignupRequest:
      type: object
      required: [orgCode, orgName, username, email, password]
      properties:
        orgCode:
          type: string
        orgName:
          type: string
        username:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
        password:
          type: string
          format: password
    InitialAdminSetupRequest:
      allOf:
        - $ref: "#/components/schemas/SignupRequest"
    SetupStatus:
      type: object
      required: [required]
      properties:
        required:
          type: boolean
    RefreshRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          type: string
    SwitchOrgRequest:
      type: object
      required: [orgId]
      properties:
        orgId:
          type: string
    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    ResetPasswordRequest:
      type: object
      required: [token, newPassword]
      properties:
        token:
          type: string
        newPassword:
          type: string
          format: password
    VerifyMFARequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
    AcceptInvitationRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        displayName:
          type: string
        password:
          type: string
          format: password
    CreateOrgRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
        name:
          type: string
    UpdateOrgRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
    InviteUserRequest:
      type: object
      required: [email, roleCode]
      properties:
        email:
          type: string
          format: email
        roleCode:
          type: string
    CreateRoleRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
        name:
          type: string
        description:
          type: string
        permissions:
          type: array
          items:
            type: string
            example: user:read
    UpdateUserRequest:
      type: object
      properties:
        status:
          type: string
          enum: [active, disabled]
        roles:
          type: array
          items:
            type: string
            example: admin
    UpdateRoleRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        permissions:
          type: array
          items:
            type: string
            example: user:read
    TokenPair:
      type: object
      required: [accessToken, accessExpiresAt, refreshToken, refreshExpiresAt]
      properties:
        accessToken:
          type: string
        accessExpiresAt:
          type: string
          format: date-time
        refreshToken:
          type: string
        refreshExpiresAt:
          type: string
          format: date-time
    NotificationDelivery:
      type: object
      properties:
        token:
          type: string
          description: 仅 debug/noop/local 通知驱动返回。
        url:
          type: string
          description: 仅 debug/noop/local 通知驱动返回。
    Principal:
      type: object
      required: [userId, orgId, sessionId, username, email]
      properties:
        userId:
          type: string
        orgId:
          type: string
        sessionId:
          type: string
        username:
          type: string
        email:
          type: string
          format: email
    MFASetup:
      type: object
      required: [secret, otpauthUrl]
      properties:
        secret:
          type: string
        otpauthUrl:
          type: string
    Organization:
      type: object
      required: [id, code, name, status, createdAt, updatedAt]
      properties:
        id:
          type: string
        code:
          type: string
        name:
          type: string
        status:
          type: string
          enum: [active, disabled, pending, used, revoked]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    User:
      type: object
      required: [id, username, email, displayName, status, mfaEnabled, createdAt, updatedAt]
      properties:
        id:
          type: string
        username:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
        status:
          type: string
          enum: [active, disabled, pending, used, revoked]
        mfaEnabled:
          type: boolean
        lockedUntil:
          type: string
          format: date-time
          nullable: true
        lastLoginAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    OrganizationUser:
      type: object
      required: [user, membershipStatus, roles]
      properties:
        user:
          $ref: "#/components/schemas/User"
        membershipStatus:
          type: string
          enum: [active, disabled]
        roles:
          type: array
          items:
            type: string
    Invitation:
      type: object
      required: [id, orgId, email, roleCode, status, invitedBy, expiresAt, createdAt, updatedAt]
      properties:
        id:
          type: string
        orgId:
          type: string
        email:
          type: string
          format: email
        roleCode:
          type: string
        status:
          type: string
          enum: [pending, used, revoked]
        invitedBy:
          type: string
        acceptedBy:
          type: string
          nullable: true
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Role:
      type: object
      required: [id, orgId, code, name, description, system, createdAt, updatedAt]
      properties:
        id:
          type: string
        orgId:
          type: string
        code:
          type: string
        name:
          type: string
        description:
          type: string
        system:
          type: boolean
        permissions:
          type: array
          items:
            type: string
            example: user:read
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Permission:
      type: object
      required: [id, code, name, description, createdAt, updatedAt]
      properties:
        id:
          type: string
        code:
          type: string
        name:
          type: string
        description:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Session:
      type: object
      required: [id, userId, orgId, userAgent, ipAddress, expiresAt, createdAt, updatedAt]
      properties:
        id:
          type: string
        userId:
          type: string
        orgId:
          type: string
        userAgent:
          type: string
        ipAddress:
          type: string
        expiresAt:
          type: string
          format: date-time
        revokedAt:
          type: string
          format: date-time
          nullable: true
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AuditLog:
      type: object
      required: [id, action, resource, resourceId, ipAddress, userAgent, metadata, createdAt]
      properties:
        id:
          type: string
        orgId:
          type: string
          nullable: true
        userId:
          type: string
          nullable: true
        action:
          type: string
        resource:
          type: string
        resourceId:
          type: string
        ipAddress:
          type: string
        userAgent:
          type: string
        metadata:
          type: string
          description: 审计服务持久化的 JSON 字符串。
        createdAt:
          type: string
          format: date-time
